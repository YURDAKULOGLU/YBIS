#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# YBIS Pre-commit Hook
# Runs quality checks before allowing commit

echo "🔍 Running pre-commit checks..."

# Run lint-staged
npx lint-staged

# Exit code will determine if commit proceeds
