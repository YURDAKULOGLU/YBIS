{
  "name": "@ybis/core",
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist",
    "test": "echo \"No tests in @ybis/core\" && exit 0",
    "lint": "eslint src --ext .ts",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "date-fns": "^4.1.0",
    "zod": "^4.1.11"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "jest": "^30.2.0",
    "typescript": "^5.3.3"
  }
}
